import {faker} from '@faker-js/faker';
import {by, expect, element} from 'detox';

describe('profile e2e tests', () => {
  it('should see user info on profile screen');

  it('should edit profile', async () => {});
});
